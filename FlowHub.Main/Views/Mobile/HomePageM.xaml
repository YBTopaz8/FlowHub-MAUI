<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FlowHub.Main.Views.Mobile.HomePageM"
             xmlns:viewModels="clr-namespace:FlowHub.Main.ViewModels"
             xmlns:models="clr-namespace:FlowHub.Models;assembly=FlowHub.Models"
             x:DataType="viewModels:HomePageVM"
             >
    <VerticalStackLayout>
        <Label FontSize="20" HorizontalTextAlignment="Center" Margin="10">
            <Label.Text>
                <MultiBinding StringFormat="{} Pocket Allowance is {0:n2} {1}">
                    <Binding Path="PocketMoney"/>
                    <Binding Path="UserCurrency"/>
                </MultiBinding>
            </Label.Text>
        </Label>
        <!-- Begin Section for Expenditure display-->
        <Border Grid.Column="0"
                    Grid.Row="0"
                Margin="10"
                Padding="5"
                BackgroundColor="Transparent"
                Stroke="DarkSlateBlue"
                StrokeThickness="2"
                StrokeShape="RoundRectangle 8">
            <VerticalStackLayout Spacing="10" Margin="6">
                <Label Text="Latest Flow Out"
                            FontSize="25"
                            HorizontalOptions="Center"
                           Margin="90,0,0,0"
                            WidthRequest="300"/>
                <BoxView
                        Color="DarkSlateBlue"
                        HeightRequest="2"
                        HorizontalOptions="Fill"/>
                <HorizontalStackLayout>
                    <Label Text="Reason: "
                           FontSize="18"
                            HorizontalOptions="Start"
                            />
                    <Label Text="{Binding ExpendituresDetails.Reason} "
                           FontSize="18"
                            HorizontalOptions="Start"
                           Margin="10,0,0,0"
                            />
                </HorizontalStackLayout>
                <HorizontalStackLayout>
                    <Label Text="Amount Spent:"
                           FontSize="18"
                            HorizontalOptions="Start"
                            />
                    <Label Text="{Binding ExpendituresDetails.AmountSpent} "
                           FontSize="18"
                            HorizontalOptions="Start"
                           Margin="10,0,0,0"
                            />
                    <Label Text="TND"
                           FontSize="18"
                            HorizontalOptions="Start"
                           Margin="5,0,0,0"
                            />
                </HorizontalStackLayout>
                <HorizontalStackLayout Margin="0,0,0,15">
                    <Label Text="Date Spent:"
                           FontSize="18"
                            HorizontalOptions="Start"
                            />
                    <Label Text="{Binding ExpendituresDetails.DateSpent, StringFormat='{0:dd MMMM, yyyy}'}"
                           FontSize="18"
                           HorizontalOptions="Start"
                           Margin="10,0,0,0"
                            />
                </HorizontalStackLayout>
            </VerticalStackLayout>

        </Border>
        <VerticalStackLayout IsVisible="false">
            <ImageButton Source="add_btn_48.png" Command="{Binding GoToAddExpenditurePageCommand}"/>
        </VerticalStackLayout>

        <!-- End Section for Expenditure display-->
    </VerticalStackLayout>
</ContentPage>